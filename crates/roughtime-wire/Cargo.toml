[package]
name = "roughtime-wire"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Roughtime wire format encode/decode (no_std compatible)"

[features]
default = ["alloc"]
alloc = []
